<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/movie_widget_item"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/colorWhite"
    android:layout_width="match_parent"
    android:layout_height="88dp"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/movie_image_widget_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/no_image_available"/>

    <!-- todo: harus dibikin widthnya jd match parent instead of wrap content cus walaupun layout_widthnya match parent tp it acts like wrap content -->
    <TextView
        android:id="@+id/movie_title_widget_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/item_section_title"
        android:textColor="@color/colorBlack"
        android:layout_gravity="center"
        android:textAlignment="center"
        android:textStyle="bold"
        tools:text="test"/>

</LinearLayout>